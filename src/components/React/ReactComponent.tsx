import React from 'react';
import styles from './styles.module.scss';

const ReactComponent = () => {
    return <div className={styles.test}>Привет из React!</div>;
};

export default ReactComponent;
